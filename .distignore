# Files excluded from the distributed plugin build (`wp dist-archive`, and the
# WordPress.org deploy). The plugin has no build step and no Composer runtime
# dependency, so the dist build is the repo minus development files.
/.git
/.github
/.gitignore
/.distignore
/.editorconfig
/composer.json
/composer.lock
/vendor
/tests
/bin
/phpunit.xml.dist
/phpunit-integration.xml.dist
/GETTING-STARTED.md
/README.md
