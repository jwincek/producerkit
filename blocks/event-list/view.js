/**
 * Event List — view module.
 *
 * Behaviour lives in the shared store so the Event Card can reuse the RSVP
 * half without a second copy of it.
 */

import '../../assets/js/interactivity/event-list.js';
