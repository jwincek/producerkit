/**
 * Event Card — view module.
 *
 * A single event with an RSVP form, so it needs the RSVP actions and nothing
 * else — no type filtering, since there is no list to filter.
 */

import '../../assets/js/interactivity/rsvp.js';
